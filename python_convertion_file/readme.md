This subdirectory contains two different pythons conversion files.
    
    cif2xrd contains conversion files to convert cif files to xrd spectras

    qeOutput2cif contains conversion files to convert Quantum Espresso geometry relaxation outputs to cif files
